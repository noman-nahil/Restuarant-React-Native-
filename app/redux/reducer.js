import * as actionTypes from './actionTypes';

const INITIAL_STATE = {
    items: []
}

export const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}